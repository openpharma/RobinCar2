# Scripts to generate test case numbers 

These are separate scripts to generate test case numbers, currently used for comparing survival function results vs. the previous `RobinCar` package's results.
